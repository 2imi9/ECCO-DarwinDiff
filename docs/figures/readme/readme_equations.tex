% readme_equations.tex -- README: the equations behind readme_method and readme_components.
%
% What it shows: every equation of the flagship DarwinDiff training step, in one figure, so the
% two diagrams above it carry none. Two columns, read left to right, top to bottom:
%   (1) bounded parameters        | (2) one forward-Euler step
%   (3) loss on the end state     | (4) the gradient, with S = dx/dtheta carried through every step
%   the objective                 | the terms of L_a, one per loss row of readme_components
%   (5) the pattern score Z, on the full width
%   (6) the error on sparse observations | (7) the simplified surface iron balance rho
% The numbers are the README's one numbering: readme_method's small tags (1)-(4) point here, and
% readme_components' row tags (1), (2), (5), (6), (7) do too. Each block is set as readme_method's
% old equation band was: a heading '(n) name' (number ink, name ddMute \fSmall), the equation in
% ink at 9 pt flush left under it, then at most three ddMute lines of 'symbol meaning . symbol
% meaning' key, every key symbol at the equations' own 9 pt (\sy). The equations are the old band's
% and the old Form column's / footer's, moved without a change to the math; only the keys'
% prose changed where it pointed across figures ('terms: next figure' -> 'terms below'), and the
% note that Carroll grading is outside L moved from under (3) to under the objective, where it
% belongs.
%
% Equations: the verified flagship set (each one executed against the code at HEAD d19e15b and
% refuted twice); notation is lightly adapted for typesetting, the math is not. Code references
% are to scripts/run_v3.0_joint_multi_aoi.py ("runner") unless a file is named; the flagship
% values live in scripts/configs/flagship_geo1.sh and no numeric parameter value appears here.
%   (1) theta_c = ell + (u - ell) (.) sigma(f_w(z_c)),
%       f_w(z) = W_3 tanh(W_2 tanh(W_1 z + b_1) + b_2) + b_3,
%       z_c = (SST_c - mean SST) / sd(SST).
%       The LINEAR bounds map for all six parameters, scav_rat included: PARAM_LOG_SCALE is unset,
%       so the log / geometric branch is inactive (log_mask=None). The three 1x1 convolutions are
%       written as the per-cell MLP they equal. mean and sd (population, ddof 0) run over the
%       ocean cells of all three regions, 0 on land; sd, not sigma, because sigma is the sigmoid.
%       src/darwindiff/carroll6.py:621-679 (bounded_params, linear map :678-679);
%       src/darwindiff/networks.py:112-135 (DINN, layers :121-129); runner :1837-1851 (shared
%       mean / std), :1886-1887 (z, 0 on land), :1983-1991 (one network per seed, the same w in
%       every region), :2550.
%   (2) x_{c,t+1} = max(0, x_{c,t} + Delta t F(x_{c,t}; theta_c, phi_c)),  x_{c,0} = x_c^IC.
%       Forward Euler, F evaluated once at x_{c,t}, theta_c the same at every step; the floor
%       max(0, .) is in the code (it never fired), so it stays in the equation. No lateral term.
%       src/darwindiff/carroll6_5pft_2layer.py:383-636 (Euler stack :610-626, floor :636);
%       runner :2025-2044 (_integrate), :814 (Delta t), :839 (T).
%       Inputs (verified by executing the flagship code): the NETWORK reads SST alone
%       (n_input_channels=1, runner :1837-1908). The BOX reads phi_c at every step: the v05 time
%       means SST_c (the SAME field as z_c's), SSS_c, wind speed_c and atmospheric pCO2_c (runner
%       :913-930, passed to every step at :2245-2250 via _integrate :2025-2044; SST also sets the
%       Eppley factor; v05 MLD is not used by the box). x_{c,0} = x_c^IC: the 10 chemistry tracers
%       (Fe, POC, PIC, DIC, ALK, both layers) from the v05 pickup (DARWIN_IC=1, runner :1720-1739,
%       build_darwin_ic_cache.py), the 5 plankton from literature constants (:1694-1702).
%   (3) L = sum_a omega_a L_a(x_{a,T}, theta_a), on the END state x_T (TIME_MEAN_LOSS is off).
%       runner :2243-2519 (aoi_loss; state = state_final :2256), :2562 (weighted region sum).
%   (4) grad_w L = sum_{a,c} [(dL/dx_{c,T}) S_{c,T} + dL/dtheta_c] dtheta_c/dw,
%       S_{c,t+1} = (dx_{c,t+1}/dx_{c,t}) S_{c,t} + dx_{c,t+1}/dtheta_c,  S_{c,0} = 0,
%       with S = dx/dtheta, the sum over the regions a and their cells c, as verified. Both
%       Jacobians are parenthesised, so no slash fraction can be read as dL/(dx S). The code gets
%       the gradient from ONE reverse-mode pass per epoch through all T steps; S is never formed,
%       and the recursion is that pass's forward-mode equivalent.
%       runner :2546-2562, :2606-2608 (total_loss.backward()); carroll6_5pft_2layer.py:610-636.
%   objective: min_w L s.t. (1), (2) at every cell c and step t < T. Only w is optimised (Adam).
%       Grading against Carroll happens after training, under no_grad, outside the objective.
%       runner :1996-2011 (Adam over the network parameters only), :2518-2608 (epoch loop),
%       :2698-2735 (post-training grading).
%   L_a = (lambda_P/11) sum_{f=1}^{11} Z(p_f, yhat_f) + lambda_2 Z_POC2 + lambda_C Z_Chl1
%       + sum_{o in O_a} lambda_o E_o + lambda_rho <rho^2>. Every weight stays symbolic, the four
%       observation weights included (o in {GT_1, GT_2, Dan, Si}; the GEOTRACES pair is written
%       sum_k lambda_{GT,k}). O_a holds only the families with cells in region a (the code gates
%       each term on n > 0: runner :2450, :2465), which key (6) says. Z_Chl1 = Z(P_d, yhat_Chl1):
%       the diatom field scored against the Chl1 target (v05's PFT-1 chlorophyll), so its line
%       writes it out. runner :2243-2519 (aoi_loss; pattern :2274-2287; POC_SUB_W :2357-2359;
%       CHL1_W_EXTRA :2363-2365). Set as the table's Form column set it: 'L_a' ink, a ddMute =
%       ahead of the first term and a ddMute + ahead of the rest, each term's name (the table
%       row's) in ddMute after it, and the tag of the equation that defines its symbol.
%   (5) Z (shape only; p = model field at x_T, y = its v05 target; means and Bessel sds over the
%       region's ocean cells Omega_a): runner :2229-2240 (term_batched), :1276-1281 (target
%       z-scored once at load). Written with the slash, one line, so nothing drops below 8 pt.
%   (6) E = sum_{c in M} (p_c - y_c)^2 / sum_{c in M} y_c^2 over the observed cells M = M_{o,a} of
%       the term's family in region a: runner :2345-2355 (GEOTRACES Fe, both layers), :2450-2456
%       (Daniels, PIC_1/POC_1), :2465-2474 with src/darwindiff/silica.py:77-108 (bSi_1 = the
%       steady-state silica diagnostic of P_d and g, not a tracer; g = theta_diatomgraz read
%       directly, runner :2469; P_d = diatom biomass at x_T, keyed once, in (5)).
%   (7) rho: a SIMPLIFIED surface-Fe balance, printed WITHOUT the temperature (Eppley) factor and
%       the diffusion term, so it is not the box's own F_Fe1/Fe_1: runner :2289-2308, against the
%       box's dDFe_1 at carroll6_5pft_2layer.py:530-535. It reads theta directly: alpha =
%       theta_alpfe, kappa = the PER-DAY scavenging rate from theta_scav_rat (the runner multiplies
%       by 86400, :2304), mu_lge = theta_Biggrow and mu_HL = theta_Smallgrow (the other mu_j are
%       fixed). Its key says "kappa per day" and "the learned mu_j" for exactly that reason.
%
% Math typesetting: readme_method's glyph set (its preamble, copied), so the figures print every
% shared symbol the same way and no Computer Modern glyph is left (pdffonts: NimbusSans, EURM,
% EUSM, EUEX only). \sansmath with the EULERGREEK option: Greek from Euler, lowercase slanted by
% 0.21 (\obl) to match the sans oblique x and w; uppercase Delta, Phi, Omega upright; nabla is
% Euler's Delta turned over; the sum is Euler's (euex); the minus and centred dot are Helvetica's
% (TS1), the < Helvetica's (T1), the slash the text slash. The elementwise product, the angle
% brackets, the element-of sign and the evaluation bar are drawn at the sans stroke weight on its
% math axis. Blocks (1)-(4) and the objective use readme_method's atoms (\qth, \qsig, ...);
% (5)-(7) and the L_a terms use readme_components' (\vth, \vsi, ..., whose \mathchoice keeps a
% Greek letter in a subscript at script size), each block exactly as its figure set it.
% 9 pt math keeps its scripts at the 8 pt floor (\DeclareMathSizes{9}{9}{8}{8}); every fraction in
% (1)-(4) is written with the slash; the display fractions of (6) and (7) keep 9 pt parts.
%
% Rules (dd-readme.sty): 176 mm wide, drawn at final size, 8 pt floor, opaque white canvas, no
% colour but ink and ddMute (no red: the gradient's colour is for the diagram's one grad path), no
% result numbers (only indices, limits, the structural 11, the zeros of (2) and (4), and the
% equation numbers). Canvas 176 mm x a whole number of bp (see \canvasH), so pdftocairo gives the
% SVG an identity transform.
\documentclass[tikz,border=0pt]{standalone}
% EULERGREEK takes ALL Greek from Euler: with eulergreek alone the uppercase Delta, Phi and Omega
% come from the OT1 sans, which has none, and they print as boxes
\PassOptionsToPackage{EULERGREEK}{sansmath}
\usepackage{dd-readme}
% ---- readme_method's math glyphs: Euler partial, ell and sum; Helvetica minus, < and centred dot;
% the text slash ------------------------------------------------------------------------------------
\DeclareMathSymbol{\eupartial}{\mathord}{EulerGreek}{"40}
\DeclareMathSymbol{\euell}{\mathord}{EulerGreek}{"60}
\DeclareSymbolFont{EulerExtension}{U}{euex}{m}{n}   % Euler's sum, not cmex's
\DeclareSymbolFont{sansTS}{TS1}{\sfdefault}{m}{n}   % Helvetica's own minus, not cmsy's
\DeclareSymbolFont{sansTo}{T1}{\sfdefault}{m}{n}    % Helvetica's own <, not cmmi's
\makeatletter
\DeclareMathSymbol{\sum@}{\mathop}{EulerExtension}{"50}
\makeatother
\DeclareMathSymbol{-}{\mathbin}{sansTS}{"3D}
\DeclareMathSymbol{<}{\mathrel}{sansTo}{"3C}
\DeclareMathSymbol{/}{\mathord}{operators}{"2F}     % the text slash
\DeclareMathSymbol{\qcdot}{\mathord}{sansTS}{"B7}   % Helvetica's centred dot, not cmsy's
% 9 pt math keeps its subscripts and fraction parts at the 8 pt floor, not the default 6 pt; the
% 8 pt headings and keys keep theirs at 8 pt, not the default 6 and 5 pt
\DeclareMathSizes{9}{9}{8}{8}
\DeclareMathSizes{8}{8}{8}{8}
% \obl{<text>}: slant a glyph by 0.21 (the sans oblique's 12 degrees) so the upright Euler glyphs
% match the oblique sans x and w. A pdfTeX text matrix: no font file, no TikZ transform.
\newcommand{\obl}[1]{\mbox{\pdfsave\pdfsetmatrix{1 0 0.21 1}\rlap{#1}\pdfrestore\phantom{#1}}}
\newcommand{\mth}{\obl{$\theta$}}
\newcommand{\mpd}{\obl{$\eupartial$}}
\newcommand{\mL}{\obl{\usefont{U}{eus}{m}{n}L}}      % Euler Script L, medium
% ---- (1)-(4) and the objective: readme_method's slanted Euler Greek, as math atoms ------------
\newcommand{\gk}[1]{\mathord{\obl{$#1$}}}
\newcommand{\qth}{\mathord{\mth}}
\newcommand{\qpd}{\mathord{\mpd}}
\newcommand{\qL}{\mathord{\mL}}
\newcommand{\qsig}{\gk{\sigma}}
\newcommand{\qphi}{\gk{\phi}}
\newcommand{\qom}{\gk{\omega}}
\newcommand{\qell}{\gk{\euell}}
% nabla: Euler's Delta turned over (Euler has no nabla of its own)
\newcommand{\qnabla}{\mathord{\raisebox{\depth}{\scalebox{1}[-1]{$\Delta$}}}}
% elementwise product: drawn, centred on Helvetica's math axis (the bar of its + and minus)
\newcommand{\qodot}{\mathbin{\tikz[baseline=0pt]{%
  \draw[line width=0.5pt] (0,2.09pt) circle (2.1pt); \fill (0,2.09pt) circle (0.62pt);}}}
\newcommand{\tsum}[1]{{\textstyle\sum_{#1}}}         % aligned cells are display style
% T after a comma in a subscript: the oblique T's left bearing opens 'a, T' wider than 'c,t'
\newcommand{\kT}{\mkern-2mu T}
% set braces from Helvetica (T1), not cmsy
\newcommand{\qset}[1]{\mathopen{\text{\textbraceleft}}#1\mathclose{\text{\textbraceright}}}
% ---- (5)-(7) and the L_a terms: readme_components' Greek, right-sized in every math style -------
\newcommand{\sG}[1]{{\mathchoice{\obl{$\displaystyle#1$}}{\obl{$\textstyle#1$}}%
  {\obl{$\scriptstyle#1$}}{\obl{$\scriptscriptstyle#1$}}}}
\newcommand{\vth}{\sG{\theta}}  \newcommand{\vrh}{\sG{\rho}}
\newcommand{\vla}{\sG{\lambda}} \newcommand{\val}{\sG{\alpha}}
\newcommand{\vka}{\sG{\kappa}}  \newcommand{\vmu}{\sG{\mu}}
\newcommand{\rmm}[1]{\mathrm{#1}}
% angle brackets, the height of a text-size delimiter, scaled with the font (9 pt math, 8 pt keys)
\makeatletter
\newdimen\qu
\newcommand{\qsz}{\qu=\f@size pt\divide\qu 9 }
\makeatother
\newcommand{\qlangle}{\mathopen{\qsz\tikz[baseline=0pt]{%
  \draw[line width=0.5pt, line join=miter] (2.35\qu,6.43\qu) -- (0.35\qu,2.09\qu) -- (2.35\qu,-2.25\qu);
  \useasboundingbox (0,-2.25\qu) rectangle (2.7\qu,6.43\qu);}}}
\newcommand{\qrangle}{\mathclose{\qsz\tikz[baseline=0pt]{%
  \draw[line width=0.5pt, line join=miter] (0.35\qu,6.43\qu) -- (2.35\qu,2.09\qu) -- (0.35\qu,-2.25\qu);
  \useasboundingbox (0,-2.25\qu) rectangle (2.7\qu,6.43\qu);}}}
% element-of, at the 8 pt script size of the sums it sits under, on the 8 pt math axis
\newcommand{\qin}{\mathrel{\tikz[baseline=0pt]{%
  \draw[line width=0.45pt] (3.9pt,3.76pt) -- (2.35pt,3.76pt)
    arc[start angle=90, end angle=270, radius=1.9pt] -- (3.9pt,-0.04pt);
  \draw[line width=0.45pt] (0.45pt,1.86pt) -- (3.5pt,1.86pt);
  \useasboundingbox (0,-0.3pt) rectangle (4.5pt,4.0pt);}}}
% evaluation bar |_{...}: a rule at the sans stroke weight, as tall as the display fraction it
% closes; the 2.5 pt kern keeps the subscript off the rule at README width
\newcommand{\qbar}{\mathclose{\vrule height 13.6pt depth 7.9pt width 0.5pt\kern2.5pt}}
% ---- headings, tags and symbol keys ------------------------------------------------------------
\newcommand{\eqtag}[1]{\textup{(#1)}}
% a heading's number: ink, so number / equation (ink, 9 pt) / key (ddMute) are three levels
\newcommand{\hnum}[1]{\textcolor{ddInk}{\eqtag{#1}}\enspace}
% a tag after an L_a term: the equation that defines its symbol, as the old Form column had it
\newcommand{\etag}[1]{\hspace{1.4mm}{\fSmall\color{ddMute}(#1)}}
% symbol key: 8 pt ddMute words, every symbol at the equations' own 9 pt (scripts at 8 pt)
\newcommand{\sy}[1]{{\fLabel$#1$}}
\newcommand{\kdot}{\enspace\textperiodcentered\enspace}
% ---- layout (mm) -------------------------------------------------------------------------------
% Canvas 176 mm x \canvasH, a whole number of bp (see the header). Everything is placed from the
% canvas top (y 0, going down) in a scope raised by \canvasH. The vertical rhythm is the old
% band's: heading -> first equation 4.0 mm, equation pitch 4.445 mm (9 pt + \jot), last equation
% -> first key line 4.26 mm, key pitch 3.5 mm, last key line -> next heading 6.53 mm; (5)-(7)
% keep the old footer's rhythm (their display fractions need more room).
\def\canvasH{131.233}   % 372 bp
\def\cA{4.0}\def\cB{90.0}           % the two columns' left edges
\def\objW{9.5mm}                    % the objective's left column: 'min_w' over 's.t.'
% row 1: (1) | (2): heading, first equation line, last key line (both columns end on it)
\def\hA{-5.0}\def\eA{-9.0}\def\kA{-25.65}
% row 2: (3) | (4)
\def\hB{-32.18}\def\eB{-36.18}
% row 3: objective | the terms of L_a (one per table row, on the table's 5.2 mm row pitch; the
% first line sits 4.6 mm under its heading so the sum's upper limit clears the heading)
\def\hC{-51.6}\def\eC{-55.6}\def\lA{-56.2}\def\lP{5.2}
\def\xOp{96.2}\def\xTm{97.8}\def\xNm{149.3}   % L_a's = / + column, the terms, their names
% row 4: (5), full width (the old footer's 4.57 / 4.445 / 3.81 mm rhythm)
\def\hD{-89.5}\def\eD{-94.07}\def\kD{-98.515}\def\kPw{3.81}
% row 5: (6) | (7) (7.11 mm heading -> equation, clearing the display fractions' numerators;
% 6.86 mm equation -> key, under the denominators and the evaluation bar's subscript)
\def\hF{-109.185}\def\eF{-116.295}\def\kF{-123.155}
\setlength{\jot}{1.6pt}

\tikzset{
  hd/.style = {font=\fSmall, text=ddMute, anchor=base west},
  eq/.style = {font=\fLabel, text=ddInk, anchor=base west},
  ky/.style = {font=\fSmall, text=ddMute, anchor=base west},
}

\begin{document}
\begin{tikzpicture}[x=1mm,y=1mm]
\sansmath
\ddcanvas{176}{\canvasH}
\begin{scope}[shift={(0,\canvasH)}, every node/.style={inner sep=0pt}]

% ============================ row 1: the forward pass =========================================
\node[hd] at (\cA,\hA) {\hnum{1}bounded parameters};
\node[eq] at (\cA,\eA) {$\begin{aligned}[t]
  \qth_c &= \qell+(u-\qell\mkern1mu)\qodot\qsig(f_w(z_c))\\
  f_w(z) &= W_3\tanh(W_2\tanh(W_1z+b_1)+b_2)+b_3\\
  z_c &= (\mathrm{SST}_c-\overline{\mathrm{SST}})/\mathrm{sd}(\mathrm{SST})
\end{aligned}$};
% the z-score's mean and sd are shared over the ocean cells of all three regions and fixed
\node[ky] at (\cA,\kA+3.5) {\sy{\qell}, \sy{u} fixed bounds\kdot\sy{\qsig} sigmoid\kdot\sy{w=\qset{W_k,b_k}}};
\node[ky] at (\cA,\kA) {\sy{\qodot} elementwise\kdot mean, sd fixed, over all regions};

\node[hd] at (\cB,\hA) {\hnum{2}one forward-Euler step};
\node[eq] at (\cB,\eA) {$\begin{aligned}[t]
  x_{c,t+1} &= \max(0,x_{c,t}+\Delta t\,F(x_{c,t};\qth_c,\qphi_c))\\
  x_{c,0} &= x_c^{\mathrm{IC}}
\end{aligned}$};
\node[ky] at (\cB,\kA+7.0) {\sy{F} box tendency\kdot\sy{\qphi_c} forcing};
\node[ky] at (\cB,\kA+3.5) {\sy{\Delta t} step length\kdot\sy{T} final step};
\node[ky] at (\cB,\kA) {IC initial condition};

% ============================ row 2: the loss and its gradient ================================
\node[hd] at (\cA,\hB) {\hnum{3}loss on the end state};
\node[eq] at (\cA,\eB) {$\qL = \tsum{a}\,\qom_a\,\qL_a(x_{a,\kT},\qth_a)$};
\node[ky] at (\cA,\eB-4.445) {\sy{a} region\kdot\sy{\qom_a} its weight};
\node[ky] at (\cA,\eB-8.89) {\sy{\qL_a} its loss, term by term below};

\node[hd] at (\cB,\hB) {\hnum{4}gradient, with \sy{S=\qpd x/\qpd\qth} carried through every step};
% the Jacobian of the first line is parenthesised like the second line's, inside Helvetica
% square brackets
\node[eq] at (\cB,\eB) {$\begin{aligned}[t]
  \qnabla_{\!w}\mkern2mu\qL &= \tsum{a,c}\,[(\qpd\qL/\qpd x_{c,\kT})\,S_{c,\kT}+\qpd\qL/\qpd\qth_c]\,\qpd\qth_c/\qpd w\\
  S_{c,t+1} &= (\qpd x_{c,t+1}/\qpd x_{c,t})\,S_{c,t}+\qpd x_{c,t+1}/\qpd\qth_c,\quad S_{c,0}=0
\end{aligned}$};

% ============================ row 3: the objective | the terms of L_a =========================
% the objective as a paper sets an optimisation problem: the constraint under it, t < T bounding
% the steps. The Carroll comparison is not a term of L: said under the objective
\node[hd] at (\cA,\hC) {objective};
\node[eq] at (\cA,\eC) {\makebox[\objW][l]{$\min\nolimits_w$}$\qL$};
\node[eq] at (\cA,\eC-4.445) {\makebox[\objW][l]{s.t.}(1), (2) at every cell $c$ and step $t<T$};
\node[ky] at (\cA,\eC-4.445-4.26) {graded against Carroll only after training, outside \sy{\qL}};

% the loss rows of readme_components as one equation: L_a (ink), a ddMute = ahead of the first
% term and a ddMute + ahead of the rest, each term's tag, then the table row's name in ddMute
\node[hd] at (\cB,\hC) {the terms of \sy{\qL_a}, one per loss row of the table};
\node[font=\fLabel, text=ddInk, anchor=base west] at (\cB,\lA) {$\qL_a$};
\node[font=\fLabel, text=ddMute, anchor=base] at (\xOp,\lA) {$=$};
\foreach \k in {1,...,5}
  \node[font=\fLabel, text=ddMute, anchor=base] at (\xOp,\lA-\k*\lP) {$+$};
\foreach \k/\t/\n in {%
    0/{$(\vla_P/11)\sum_{f=1}^{11}Z(p_f,\hat y_f)$\etag{5}}/{pattern, shape only},
    1/{$\vla_2Z_{\rmm{POC}_2}+\vla_CZ(P_{\rmm d},\hat y_{\rmm{Chl}_1})$\etag{5}}/{extra pattern terms},
    2/{$\sum_{k=1}^{2}\vla_{\rmm{GT},k}E(\rmm{Fe}_k,y^{\rmm{GT}}_k)$\etag{6}}/{dissolved iron},
    3/{$\vla_{\rmm{Dan}}E(\rmm{PIC}_1/\rmm{POC}_1,\,y^{\rmm{Dan}})$\etag{6}}/{calcite ratio},
    4/{$\vla_{\rmm{Si}}E(\rmm{bSi}_1(P_{\rmm d},g),\,y^{\rmm{Si}})$\etag{6}}/{biogenic silica},
    5/{$\vla_{\vrh}\qlangle\vrh^2\qrangle_{\Omega_a}$\etag{7}}/{iron-budget residual}} {%
  \node[eq] at (\xTm,\lA-\k*\lP) {\t};
  \node[ky] at (\xNm,\lA-\k*\lP) {\n};}

% ============================ row 4: (5), on the full width ====================================
% Its bars and s are per region (Omega_a); the SST z-score's are not. Chl_1 is v05's diatom
% chlorophyll (PFT 1), not the surface layer: its key says so
\node[hd] at (\cA,\hD) {\hnum{5}pattern score, shape only};
\node[eq] at (\cA,\eD) {$Z(p,\hat y)=\qlangle((p_c-\bar p)/s_p-\hat y_c)^2\qrangle_{\Omega_a},\quad \hat y_c=(y_c-\bar y)/s_y$};
\node[ky] at (\cA,\kD) {\sy{p_f} field \sy{f} at \sy{x_T}\kdot\sy{y_f} its v05 target\kdot\sy{Z_X=Z(X,\hat y_X)}\kdot\sy{P_{\rmm d}} diatoms\kdot\sy{\rmm{Chl}_1} v05 diatom chlorophyll};
\node[ky] at (\cA,\kD-\kPw) {\sy{\qlangle\qcdot\qrangle}, \sy{\bar p}, \sy{\bar y}, \sy{s}: mean, sample sd over \sy{\Omega_a}, the ocean cells of region \sy{a}\kdot\sy{z_c} uses the mean, sd over all regions};

% ============================ row 5: (6) | (7) ================================================
% (6): bSi_1 is a steady-state diagnostic of P_d and g, not a model tracer
\node[hd] at (\cA,\hF) {\hnum{6}error on sparse observations};
\node[eq] at (\cA,\eF) {$E(p,y)=\dfrac{\sum_{c\qin M}(p_c-y_c)^2}{\sum_{c\qin M}y_c^2}$};
\node[ky] at (\cA,\kF) {\sy{p} at \sy{x_T}, \sy{y} observed\kdot\sy{M} its cells in region \sy{a} (if none, no term)};
\node[ky] at (\cA,\kF-\kPw) {\sy{\rmm{bSi}_1} steady-state silica diagnostic\kdot\sy{g=\vth_{\rmm{diatomgraz}}}};
% (7): without the temperature factor and diffusion, so named simplified
\node[hd] at (\cB,\hF) {\hnum{7}surface iron balance, simplified};
\node[eq] at (\cB,\eF) {$\vrh_c=\dfrac{\val\Phi_{\rmm d}-\vka\,\rmm{Fe}_1\rmm{POC}_1-Q_{\rmm{Fe}}\,f_{\rmm{Fe}}\sum_j\vmu_jP_j}{\rmm{Fe}_1}\,\qbar_{x_{c,\kT}}$};
\node[ky] at (\cB,\kF) {no temperature factor, no diffusion\kdot\sy{\vka} per day};
\node[ky] at (\cB,\kF-\kPw) {\sy{\vth_c} sets \sy{\val}, \sy{\vka}, the learned \sy{\vmu_j}\kdot\sy{P_j} plankton\kdot\sy{f_{\rmm{Fe}}} iron limitation};

\end{scope}
\end{tikzpicture}
\end{document}
