# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
build/
dist/
.eggs/
.installed.cfg
MANIFEST

# Virtualenvs
.venv/
venv/
env/
ENV/

# uv
.uv/

# Notebooks
.ipynb_checkpoints/
*.ipynb_checkpoints

# Tests / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.hypothesis/
.mypy_cache/
.ruff_cache/

# Data — keep directory but not contents
data/*
!data/.gitkeep
!data/README.md

# References — store citations in README, not the PDFs or external repos themselves
references/*.pdf
references/ecco_darwin/

# Models / checkpoints
*.pt
*.pth
*.ckpt
*.safetensors
checkpoints/
runs/
wandb/
mlruns/

# Logs
*.log
logs/

# Per-seed ensemble checkpoints (regeneratable from the owning notebook;
# typically hundreds of KB per file, not worth committing). Summary JSONs
# and figures next to them ARE committed.
notebooks/nb17_results/seedA_*.npz
notebooks/nb17_results/seedB_*.npz

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env
.env
.env.local
*.env

# Agent worktree settings (per-machine, not project content) — but keep
# the project-scoped skill bundle, which ships with the repo. Note: parent
# dir cannot be excluded with `.claude/` if we want to re-include
# `.claude/skills/`; use `.claude/*` instead.
.claude/*
!.claude/skills/
!.claude/skills/**

# Overnight automation logs
scripts/overnight_log.txt

# Local-only working notes and scratch (kept on disk, not part of the public repo)
/compute_envelope.py
/docs/CLAUDE_PROJECT_CONTEXT.md
/docs/briefs/
/docs/research_notes/2026-05-03_overnight.md
/docs/research_notes/2026-05-21_jon_meeting_brief.md
/docs/research_notes/2026-05-23_cleanup_audit.md
/docs/research_notes/2026-05-23_compute_ladder.md
/docs/research_notes/2026-05-23_overnight_diff_full_darwin.md
/docs/research_notes/2026-05-23_overnight_executed.md
/docs/research_notes/2026-06-06_daily_data_defer.md

# Draft manuscript — kept local + in git history, not in the public default tree
/docs/paper/

# Talks: working presentation bundle (decks/scripts), never upload
/docs/talks/

# Decorative project art (rendered figures + build scaffold)
/docs/research_notes/art/

# Binary IC caches — regenerable via build_darwin_ic_cache.py; runtime-loaded
# from disk by exact name, so kept locally but never tracked
scripts/*.npz

# Repo-local gitignored working-notes home (non-core material lives here)
/notes/

# MkDocs local build output (the published site is built by Read the Docs)
/site/

# Apptainer/Singularity images + build artifacts (images are multi-GB and are
# built on the cluster; rebuild from containers/darwindiff.def)
containers/*.sif
containers/build_*.out
containers/.sif_jobid

# Local probe/diagnostic netCDF dumps written to the repo root by the UDE and
# emulator probe scripts (183 MB as of 2026-07-19). Regenerate with the probe
# scripts in scripts/; never commit.
/probe_*.nc

# Manuscript is LOCAL-ONLY by user directive (see also /docs/paper/ above).
# A root-level copy kept appearing as untracked and was one `git add .` away
# from being committed against that directive.
/main.tex
/main.pdf

# Third-party figures pulled from published papers (FNO / GINO / SFNO / CES /
# UDE-SciML) kept locally as reading references for the neural-operator scan.
# They are NOT our work and must never enter this public repo's history.
docs/figures/refs/
.claude/skills/tikz/

# Paper/abstract drafts are LOCAL-ONLY by user directive (2026-07-28).
/docs/research_notes/*workshop_paper*.md
